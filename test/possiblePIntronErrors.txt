//possiblePIntronErrors.txt

//omitted from io-multifasta_test.c
/*	DUPLICATE LINE OF CODE FOUND AT: LINE 57 OF io-multifasta.c
	LINES 57-58 equal to LINES 41-42 in io-multifasta.c

	verify that the function get_Complement return the correct complement

	Test(ioMultifastaTest,getComplementTestX) {
		cr_expect(get_Complement('A')=='T');
	}
*/

//omitted from refine-intron_test.c
/*	getBursetFrequency POSSIBLE ERROR FOUND AT: FROM LINE 381 OF refine-intron.c

	create variables needed for the function getBursetFrequency,
	set these variables with right values,
	verify that the function with these variables returns 1 (in tests from 1 to 5),
	verify that the function with these variables returns 5 (in test 6),
	but the function returns 0 in every test (UNEXPECTED)

	Test(refineIntronTest,getBursetFrequencyTest) {
		char gsd=*"AA";
		char gsa=*"AG";
		cr_expect(getBursetFrequency(&gsd,&gsa)==1);
	}
	Test(refineIntronTest,getBursetFrequencyTest2) {
		char gsd=*"AA";
		char gsa=*"AT";
		cr_expect(getBursetFrequency(&gsd,&gsa)==1);
	}
	Test(refineIntronTest,getBursetFrequencyTest3) {
		char gsd=*"AA";
		char gsa=*"GT";
		cr_expect(getBursetFrequency(&gsd,&gsa)==1);
	}
	Test(refineIntronTest,getBursetFrequencyTest4) {
		char gsd=*"AC";
		char gsa=*"CC";
		cr_expect(getBursetFrequency(&gsd,&gsa)==1);
	}
	Test(refineIntronTest,getBursetFrequencyTest5) {
		char gsd=*"AG";
		char gsa=*"AC";
		cr_expect(getBursetFrequency(&gsd,&gsa)==1);
	}
	Test(refineIntronTest,getBursetFrequencyTest6) {
		char gsd=*"AG";
		char gsa=*"AG";
		cr_expect(getBursetFrequency(&gsd,&gsa)==5);
	}
*/
